<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>【SICP】 test_3_23</title>
    <url>/p/d9efbaf4.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>用序列实现双端队列 deque ，即可从前或从后插入或删除。<ul>
<li>构造函数 <code>make_deque</code> ;</li>
<li>选择函数 <code>front_deque</code> 、 <code>rear_deque</code> ;</li>
<li>改变函数 <code>front_isnert_deque!</code> 、<code>rear_insert_deque!</code> 、<code>front_delete_deque!</code> 、<code>rear_delete_deque!</code> 。</li>
</ul>
</li>
<li>要求是所有操作都需要在 <code>O(1)</code> 步骤内完成。</li>
</ul>
<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><ul>
<li>SIPC 3.3.2 实现了队列 queue ，其为链表实现，队列的链表节点包括 data 指针和 next 指针：<img src="/p/d9efbaf4/SICP_queue.jpg" alt="SICP_queue"></li>
<li><p>所以不难想象出要实现双端队列，其节点需要三个指针，包括 data 指针、next 指针和 last 指针：<img src="/p/d9efbaf4/SICP_deque.jpg" alt="SICP_deque"></p>
</li>
<li><p>有了前面序列实现双端队列节点的假设之后，其他操作都比较容易推导写出，但这里还存在一个问题，即需要重现实现 deque 的打印。</p>
<ul>
<li>要注意的是，为了结果展示，在所有改变函数中最后都会返回打印结果，但原改变操作仍然是 <code>O(1)</code> 时间内完成的。</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight scheme"><table><tr><td class="code"><pre><span class="line"><span class="comment">;; 已跑通，但很多冗余代码</span></span><br><span class="line"></span><br><span class="line"><span class="comment">;; 制造一个双端节点</span></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">node</span>) (<span class="name"><span class="built_in">cons</span></span> nil (<span class="name"><span class="built_in">cons</span></span> nil nil)))</span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">set_node_val!</span> n x) (<span class="name"><span class="built_in">set-car!</span></span> n x))</span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">last_ptr</span> n) (<span class="name"><span class="built_in">cadr</span></span> n)) <span class="comment">;; 当前节点的前一个</span></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">next_ptr</span> n) (<span class="name">cddr</span> n)) <span class="comment">;; 当前节点的下一个</span></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">connect_two_node</span> nl nr)</span><br><span class="line">  (<span class="name"><span class="built_in">begin</span></span> (<span class="name"><span class="built_in">set-car!</span></span> (<span class="name"><span class="built_in">cdr</span></span> nr) nl)</span><br><span class="line">         (<span class="name"><span class="built_in">set-cdr!</span></span> (<span class="name"><span class="built_in">cdr</span></span> nl) nr)</span><br><span class="line">         <span class="symbol">&#x27;OK</span>))</span><br><span class="line"></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">make_deque</span>) (<span class="name"><span class="built_in">cons</span></span> nil nil))</span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">front_ptr</span> d) (<span class="name"><span class="built_in">car</span></span> d))</span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">rear_ptr</span> d) (<span class="name"><span class="built_in">cdr</span></span> d))</span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">set_front_ptr!</span> d n) (<span class="name"><span class="built_in">set-car!</span></span> d n))</span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">set_rear_ptr!</span> d n) (<span class="name"><span class="built_in">set-cdr!</span></span> d n))</span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">empty_deque?</span> d)</span><br><span class="line">  (<span class="name"><span class="built_in">or</span></span> (<span class="name"><span class="built_in">null?</span></span> (<span class="name">front_ptr</span> d)) (<span class="name"><span class="built_in">null?</span></span> (<span class="name">rear_ptr</span> d))))</span><br><span class="line"></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">clear_deque</span> d)</span><br><span class="line">  (<span class="name"><span class="built_in">begin</span></span> (<span class="name"><span class="built_in">set-car!</span></span> d nil)</span><br><span class="line">         (<span class="name"><span class="built_in">set-cdr!</span></span> d nil)</span><br><span class="line">         <span class="symbol">&#x27;OK</span>))</span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">insert_one_element!</span> d x)</span><br><span class="line">  (<span class="name"><span class="built_in">let</span></span> ((<span class="name">new_node</span> (<span class="name">node</span>)))</span><br><span class="line">    (<span class="name"><span class="built_in">begin</span></span> (<span class="name">set_front_ptr!</span> d new_node)</span><br><span class="line">           (<span class="name">set_rear_ptr!</span> d new_node)</span><br><span class="line">           (<span class="name">set_node_val!</span> new_node x)</span><br><span class="line">           <span class="symbol">&#x27;OK</span>)))</span><br><span class="line"></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">front_insert_deque!</span> d x)</span><br><span class="line">  (<span class="name"><span class="built_in">cond</span></span> ((<span class="name">empty_deque?</span> d)</span><br><span class="line">         (<span class="name"><span class="built_in">begin</span></span> (<span class="name">clear_deque</span> d)</span><br><span class="line">                (<span class="name">insert_one_element!</span> d x)</span><br><span class="line">                (<span class="name">print_deque</span> d)))</span><br><span class="line">        (<span class="name"><span class="built_in">else</span></span></span><br><span class="line">         (<span class="name"><span class="built_in">let</span></span> ((<span class="name">new_node</span> (<span class="name">node</span>)))</span><br><span class="line">           (<span class="name"><span class="built_in">begin</span></span> (<span class="name">set_node_val!</span> new_node x)</span><br><span class="line">                  (<span class="name">connect_two_node</span> new_node (<span class="name">front_ptr</span> d))</span><br><span class="line">                  (<span class="name">set_front_ptr!</span> d new_node)</span><br><span class="line">                  (<span class="name">print_deque</span> d))))))</span><br><span class="line"></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">rear_insert_deque!</span> d x)</span><br><span class="line">  (<span class="name"><span class="built_in">cond</span></span> ((<span class="name">empty_deque?</span> d)</span><br><span class="line">         (<span class="name"><span class="built_in">begin</span></span> (<span class="name">clear_deque</span> d)</span><br><span class="line">                (<span class="name">insert_one_element!</span> d x)</span><br><span class="line">                (<span class="name">print_deque</span> d)))</span><br><span class="line">        (<span class="name"><span class="built_in">else</span></span></span><br><span class="line">         (<span class="name"><span class="built_in">let</span></span> ((<span class="name">new_node</span> (<span class="name">node</span>)))</span><br><span class="line">           (<span class="name"><span class="built_in">begin</span></span> (<span class="name">set_node_val!</span> new_node x)</span><br><span class="line">                  (<span class="name">connect_two_node</span> (<span class="name">rear_ptr</span> d) new_node)</span><br><span class="line">                  (<span class="name">set_rear_ptr!</span> d new_node)</span><br><span class="line">                  (<span class="name">print_deque</span> d))))))</span><br><span class="line"></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">front_delete_deque!</span> d)</span><br><span class="line">  (<span class="name"><span class="built_in">cond</span></span> ((<span class="name">empty_deque?</span> d) (<span class="name">error</span> <span class="string">&quot;delete error&quot;</span>))</span><br><span class="line">        ((<span class="name"><span class="built_in">eq?</span></span> (<span class="name">front_ptr</span> d) (<span class="name">rear_ptr</span> d)) (<span class="name"><span class="built_in">begin</span></span> (<span class="name">clear_deque</span> d)</span><br><span class="line">                                                 (<span class="name">print_deque</span> d)))</span><br><span class="line">        (<span class="name"><span class="built_in">else</span></span></span><br><span class="line">         (<span class="name"><span class="built_in">begin</span></span> (<span class="name">set_front_ptr!</span> d (<span class="name">next_ptr</span> (<span class="name">front_ptr</span> d)))</span><br><span class="line">                (<span class="name"><span class="built_in">set-car!</span></span> (<span class="name"><span class="built_in">cdr</span></span> (<span class="name">front_ptr</span> d)) nil)</span><br><span class="line">                (<span class="name">print_deque</span> d)))))</span><br><span class="line"></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">rear_delete_deque!</span> d)</span><br><span class="line">  (<span class="name"><span class="built_in">cond</span></span> ((<span class="name">empty_deque?</span> d) (<span class="name">error</span> <span class="string">&quot;delete error&quot;</span>))</span><br><span class="line">        ((<span class="name"><span class="built_in">eq?</span></span> (<span class="name">front_ptr</span> d) (<span class="name">rear_ptr</span> d)) (<span class="name"><span class="built_in">begin</span></span> (<span class="name">clear_deque</span> d)</span><br><span class="line">                                                 (<span class="name">print_deque</span> d)))</span><br><span class="line">        (<span class="name"><span class="built_in">else</span></span></span><br><span class="line">         (<span class="name"><span class="built_in">begin</span></span> (<span class="name">set_rear_ptr!</span> d (<span class="name">last_ptr</span> (<span class="name">rear_ptr</span> d)))</span><br><span class="line">                (<span class="name"><span class="built_in">set-cdr!</span></span> (<span class="name"><span class="built_in">cdr</span></span> (<span class="name">rear_ptr</span> d)) nil)</span><br><span class="line">                (<span class="name">print_deque</span> d)))))</span><br><span class="line"></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> (<span class="name">print_deque</span> d)</span><br><span class="line"></span><br><span class="line">  (<span class="name"><span class="built_in">define</span></span> a (<span class="name">front_ptr</span> d))</span><br><span class="line">  (<span class="name"><span class="built_in">define</span></span> b (<span class="name">rear_ptr</span> d))</span><br><span class="line">  </span><br><span class="line">  (<span class="name"><span class="built_in">define</span></span> (<span class="name">iter</span> ptr)</span><br><span class="line">    (<span class="name"><span class="built_in">if</span></span> (<span class="name"><span class="built_in">null?</span></span> ptr)</span><br><span class="line">        nil</span><br><span class="line">        (<span class="name"><span class="built_in">cons</span></span> (<span class="name"><span class="built_in">car</span></span> ptr) (<span class="name">iter</span> (<span class="name">next_ptr</span> ptr)))))</span><br><span class="line">  </span><br><span class="line">  (<span class="name">iter</span> a))</span><br><span class="line"></span><br><span class="line">(<span class="name"><span class="built_in">define</span></span> d (<span class="name">make_deque</span>))</span><br><span class="line">d</span><br><span class="line">(<span class="name">rear_insert_deque!</span> d <span class="symbol">&#x27;a</span>)</span><br><span class="line">(<span class="name">rear_insert_deque!</span> d <span class="symbol">&#x27;b</span>)</span><br><span class="line">(<span class="name">rear_insert_deque!</span> d <span class="symbol">&#x27;c</span>)</span><br><span class="line">(<span class="name">front_insert_deque!</span> d <span class="symbol">&#x27;d</span>)</span><br><span class="line"></span><br><span class="line">(<span class="name">front_delete_deque!</span> d)</span><br><span class="line">(<span class="name">rear_delete_deque!</span> d)</span><br><span class="line">(<span class="name">front_delete_deque!</span> d)</span><br><span class="line">(<span class="name">empty_deque?</span> d)</span><br><span class="line">(<span class="name">rear_delete_deque!</span> d)</span><br><span class="line">(<span class="name">empty_deque?</span> d)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>SICP</category>
        <category>test_3_23</category>
      </categories>
      <tags>
        <tag>SICP</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/7/25】919.完全二叉树插入器</title>
    <url>/p/3aca790c.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>实现 <code>CBTInserter</code> 类的操作：<ul>
<li><code>CBTInserter(TreeNode root)</code> 使用完全二叉树 <code>root</code> 来对该数据结构进行初始化。</li>
<li><code>CBTInserter.insert(int v)</code> 向该数据结构插入一个值为 <code>v</code> 的新节点，插入后仍保持完全二叉树状态，并且返回插入节点的父节点的值。</li>
<li><code>CBTInserter.get_root()</code> 返回完全二叉树的头节点。</li>
</ul>
</li>
</ul>
<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><ul>
<li>用数组来保存完全二叉树。【数组的索引可以用来确定节点在完全二叉树中的顺序】<ul>
<li>插入一个新值，就相当于在数组尾部插入一个节点，然后根据索引计算得到 <code>parent</code> 的索引。</li>
<li>还要将当前新值连接到真正的树上，找到 <code>parent</code> 索引之后，就可以根据当前索引是奇数还是偶数来判断是连接到 <code>parent</code> 的左子还是右子。【因为 <code>root</code> 是索引 <code>0</code>，所以当前索引为奇数则是父节点的左子，当前索引为偶数则是父节点的右子】</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">CBTInserter</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    vector&lt;TreeNode*&gt; vec;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">CBTInserter</span>(TreeNode* root)</span><br><span class="line">    &#123;</span><br><span class="line">        queue&lt;TreeNode*&gt; q;</span><br><span class="line">        q.<span class="built_in">push</span>(root);</span><br><span class="line">        <span class="keyword">while</span>(!q.<span class="built_in">empty</span>())</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> sz = q.<span class="built_in">size</span>();</span><br><span class="line">            <span class="keyword">while</span>(sz-- &gt; <span class="number">0</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                TreeNode* tmp = q.<span class="built_in">front</span>(); q.<span class="built_in">pop</span>();</span><br><span class="line">                vec.<span class="built_in">push_back</span>(tmp);</span><br><span class="line">                <span class="keyword">if</span>(tmp-&gt;left) q.<span class="built_in">push</span>(tmp-&gt;left);</span><br><span class="line">                <span class="keyword">if</span>(tmp-&gt;right) q.<span class="built_in">push</span>(tmp-&gt;right);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">insert</span><span class="params">(<span class="type">int</span> val)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        vec.<span class="built_in">push_back</span>(<span class="keyword">new</span> <span class="built_in">TreeNode</span>(val));</span><br><span class="line">        <span class="type">int</span> idx = vec.<span class="built_in">size</span>() - <span class="number">1</span>;</span><br><span class="line">        <span class="type">int</span> parent_idx = (idx - <span class="number">1</span>) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span>(idx &amp; <span class="number">1</span>) vec[parent_idx]-&gt;left = vec[idx];</span><br><span class="line">        <span class="keyword">else</span> vec[parent_idx]-&gt;right = vec[idx];</span><br><span class="line">        <span class="keyword">return</span> vec[parent_idx]-&gt;val;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function">TreeNode* <span class="title">get_root</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> vec[<span class="number">0</span>];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/7/27】592.分数加减运算</title>
    <url>/p/d930f5e8.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>字符串中只包含分数和加减运算，返回字符串的计算结果。<ul>
<li>输入和输出分数格式均为 <code>±分子/分母</code>。</li>
<li>第一个分数的正号会被省略。</li>
<li>输入中只包含合法的最简分数。</li>
</ul>
</li>
</ul>
<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><ul>
<li>字符串解析求公式，一般都是直接做模拟。<ul>
<li>利用变量 <code>num</code> 来保存当前数值。</li>
<li>利用变量 <code>neg</code> 来保存当前数的正负（注意分母均是正数）。</li>
<li>把分数用分母和分子两个数值来表示。</li>
</ul>
</li>
<li>模拟思路如下：【可以用状态机的状态转移思想来理解整个模拟过程】<ol>
<li>遇到 <code>+</code> 或 <code>-</code> ，说明前一个分数的分母结束保存【把当前保存数值 <code>num</code> 存入分母中，并将 <code>num</code> 置为 <code>0</code>】，接着根据符号将 <code>neg</code> 置位；<ul>
<li>与此同时还需要将<strong>前两个分数</strong>相加。【两分数计算后求得分子和分母的 gcd 结果（即最大公约数），根据结果进行约分】</li>
</ul>
</li>
<li>遇到 <code>/</code> ，说明当前分数的分子结束保存【把当前保存数值 <code>num</code> 存入分子中，并将 <code>num</code> 置为 <code>0</code>】</li>
<li>遇到数值就更新 <code>num</code> 即可。</li>
</ol>
</li>
<li>本题稍微巧妙一点的方法在于如何处理边界。<ul>
<li>如在处理 <code>-1/2 +1/2</code> 的时候，可以认为是 <code>0/1 -1/2 +1/2</code> 的计算结果，如此便不用特别处理第一个分数。</li>
<li>如处理 <code>1/3 -1/2</code> 的时候，可以认为是 <code>+1/3 -1/2</code> ，即往前面补多一个正号，保证第一个分数的处理和后面的处理是一致的。【即使 <code>-1/2 +1/2</code> 也可以视为 <code>+ -1/2 +1/2</code>】【同理可以在最后加上正号。逻辑上就是为了方便处理字符串中最后一个分母】</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// lu：left up，第一个分数的分子</span></span><br><span class="line">    <span class="comment">// ld：left down，第一个分数的分母</span></span><br><span class="line">    <span class="comment">// ru：right up，第二个分数的分子</span></span><br><span class="line">    <span class="comment">// rd：right down，第二个分数的分母</span></span><br><span class="line">    <span class="type">int</span> lu = <span class="number">0</span>, ld = <span class="number">1</span>, ru = <span class="number">0</span>, rd = <span class="number">1</span>;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">gcd</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> b&gt;<span class="number">0</span> ? <span class="built_in">gcd</span>(b,a%b):a;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">count</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="type">int</span> u = lu * rd + ru * ld;</span><br><span class="line">        <span class="type">int</span> d = ld * rd;</span><br><span class="line">        lu = u;</span><br><span class="line">        ld = d;</span><br><span class="line">        <span class="type">int</span> tmp = <span class="built_in">gcd</span>(<span class="built_in">abs</span>(lu), ld);</span><br><span class="line">        <span class="keyword">if</span>(tmp == <span class="number">0</span>) <span class="keyword">return</span>;</span><br><span class="line">        lu /= tmp;</span><br><span class="line">        ld /= tmp;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">string <span class="title">fractionAddition</span><span class="params">(string expression)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="type">int</span> m = expression.<span class="built_in">size</span>();</span><br><span class="line">        <span class="type">int</span> neg = <span class="number">1</span>;</span><br><span class="line">        <span class="type">int</span> num = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">-1</span>; i &lt;= m; ++i)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">char</span> ch = i == <span class="number">-1</span> <span class="keyword">or</span> i == m? <span class="string">&#x27;+&#x27;</span> : expression[i];</span><br><span class="line">            <span class="keyword">if</span>(ch == <span class="string">&#x27;-&#x27;</span> <span class="keyword">or</span> ch == <span class="string">&#x27;+&#x27;</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span>(i != <span class="number">-1</span> <span class="keyword">and</span> i != <span class="number">0</span>) rd = num;</span><br><span class="line">                <span class="built_in">count</span>();</span><br><span class="line">                neg = ch == <span class="string">&#x27;-&#x27;</span> ? <span class="number">-1</span> : <span class="number">1</span>;</span><br><span class="line">                num = <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(ch == <span class="string">&#x27;/&#x27;</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                ru = num * neg;</span><br><span class="line">                num = <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span>(ch &gt;= <span class="string">&#x27;0&#x27;</span> <span class="keyword">and</span> ch &lt;= <span class="string">&#x27;9&#x27;</span>)</span><br><span class="line">                num = num * <span class="number">10</span> + (<span class="type">int</span>)(ch - <span class="string">&#x27;0&#x27;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        string ans = lu &lt; <span class="number">0</span> ? <span class="string">&quot;-&quot;</span> : <span class="string">&quot;&quot;</span>;</span><br><span class="line">        ans += <span class="built_in">to_string</span>(<span class="built_in">abs</span>(lu)) + <span class="string">&quot;/&quot;</span> + <span class="built_in">to_string</span>(ld);</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/7/28】1331. 数组序号转换</title>
    <url>/p/4c1de1ba.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个整数数组，将数组中每个元素替换为排序后的序号。<ul>
<li>数组中有重复值，重复值有相同的序号。</li>
<li>序号从 <code>1</code> 开始。</li>
</ul>
</li>
</ul>
<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><ul>
<li>该题理解映射关系即可。<ul>
<li>用 <code>map</code> 来进行排序，其中 <code>map</code> 的 <code>key</code> 为数组中的数值，<code>value</code> 为该数值的所有索引（因为可能重复，所以索引的保存用 <code>vector</code>），即用 <code>map&lt;int, vector&lt;int&gt;&gt;</code> 来保存映射关系。</li>
<li>存入 <code>map</code> 之后再进行一次遍历即可取出排序后的序号。<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2></li>
</ul>
</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">arrayRankTransform</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; arr)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        map&lt;<span class="type">int</span>, vector&lt;<span class="type">int</span>&gt;&gt; mapped;</span><br><span class="line">        <span class="type">int</span> i;</span><br><span class="line">        <span class="keyword">for</span>(i = <span class="number">0</span>; i &lt; arr.<span class="built_in">size</span>(); ++i)</span><br><span class="line">            mapped[arr[i]].<span class="built_in">push_back</span>(i);</span><br><span class="line">        i = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">auto</span>&amp; [_, vec] : mapped)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> idx : vec) arr[idx] = i;</span><br><span class="line">            ++i;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> arr;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/7/29】593. 有效的正方形</title>
    <url>/p/5fa4169f.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定四个点，判断是否构成一个正方形。</li>
</ul>
<h2 id="解题思路："><a href="#解题思路：" class="headerlink" title="解题思路："></a>解题思路：</h2><ul>
<li>统计<strong>正方形四条边</strong>和<strong>两条对角线</strong>的长度，并存入 <code>set</code> 中去重。<ul>
<li>最后 <code>set</code> 中不存在长度为 <code>0</code> 的边；【即避免出现点重合的情况】</li>
<li>且 <code>set</code> 大小为 <code>2</code>。【即只存在两种长度】</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">count_len</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; vec)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> vec[<span class="number">0</span>]*vec[<span class="number">0</span>]+vec[<span class="number">1</span>]*vec[<span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">validSquare</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; p1, vector&lt;<span class="type">int</span>&gt;&amp; p2, vector&lt;<span class="type">int</span>&gt;&amp; p3, vector&lt;<span class="type">int</span>&gt;&amp; p4)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        unordered_set&lt;<span class="type">int</span>&gt; ss;</span><br><span class="line">        vector&lt;<span class="type">int</span>&gt; vec = &#123;p2[<span class="number">0</span>] - p1[<span class="number">0</span>], p2[<span class="number">1</span>] - p1[<span class="number">1</span>]&#125;; ss.<span class="built_in">insert</span>(<span class="built_in">count_len</span>(vec));</span><br><span class="line">        vec = &#123;p3[<span class="number">0</span>] - p1[<span class="number">0</span>], p3[<span class="number">1</span>] - p1[<span class="number">1</span>]&#125;; ss.<span class="built_in">insert</span>(<span class="built_in">count_len</span>(vec));</span><br><span class="line">        vec = &#123;p4[<span class="number">0</span>] - p1[<span class="number">0</span>], p4[<span class="number">1</span>] - p1[<span class="number">1</span>]&#125;; ss.<span class="built_in">insert</span>(<span class="built_in">count_len</span>(vec));</span><br><span class="line">        vec = &#123;p3[<span class="number">0</span>] - p2[<span class="number">0</span>], p3[<span class="number">1</span>] - p2[<span class="number">1</span>]&#125;; ss.<span class="built_in">insert</span>(<span class="built_in">count_len</span>(vec));</span><br><span class="line">        vec = &#123;p4[<span class="number">0</span>] - p3[<span class="number">0</span>], p4[<span class="number">1</span>] - p3[<span class="number">1</span>]&#125;; ss.<span class="built_in">insert</span>(<span class="built_in">count_len</span>(vec));</span><br><span class="line">        vec = &#123;p4[<span class="number">0</span>] - p2[<span class="number">0</span>], p4[<span class="number">1</span>] - p2[<span class="number">1</span>]&#125;; ss.<span class="built_in">insert</span>(<span class="built_in">count_len</span>(vec));</span><br><span class="line">        <span class="keyword">return</span> !ss.<span class="built_in">count</span>(<span class="number">0</span>) <span class="keyword">and</span> ss.<span class="built_in">size</span>() == <span class="number">2</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/7/31】1161. 最大层内元素和</title>
    <url>/p/bd0155e2.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个二叉树的根节点 <code>root</code>，返回层内元素之和最大的层号。<ul>
<li>如果和最大的层号有好几个，则返回最小的那个层号。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>层次遍历基础应用题。<ul>
<li>遍历过程中维护层数和当前层的求和值即可。</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">maxLevelSum</span><span class="params">(TreeNode* root)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        queue&lt;TreeNode*&gt; q;</span><br><span class="line">        q.<span class="built_in">push</span>(root);</span><br><span class="line">        <span class="type">int</span> cur_sum = INT_MIN;</span><br><span class="line">        <span class="type">int</span> level = <span class="number">1</span>;</span><br><span class="line">        <span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span>(!q.<span class="built_in">empty</span>())</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> sz = q.<span class="built_in">size</span>();</span><br><span class="line">            <span class="type">int</span> sum = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">while</span>(sz-- &gt; <span class="number">0</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                TreeNode* tmp = q.<span class="built_in">front</span>(); q.<span class="built_in">pop</span>();</span><br><span class="line">                sum += tmp-&gt;val;</span><br><span class="line">                <span class="keyword">if</span>(tmp-&gt;left) q.<span class="built_in">push</span>(tmp-&gt;left);</span><br><span class="line">                <span class="keyword">if</span>(tmp-&gt;right) q.<span class="built_in">push</span>(tmp-&gt;right);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(sum &gt; cur_sum) ans = level, cur_sum = sum;</span><br><span class="line">            ++level;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/7/30】952. 按公因数计算最大组件大小</title>
    <url>/p/691feb55.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个数组 <code>nums</code>，当 <code>nums[i]</code> 和 <code>nums[j]</code> 有一个大于 <code>1</code> 的公因数时，认为两个数存在有一条边，返回图中最大连通组件的大小。<ul>
<li>满足 $ 1 &lt;= nums.length &lt;= 2 * 10^4 $。</li>
<li>满足 $ 1 &lt;= nums[i] &lt;= 10^5 $。</li>
<li><code>nums</code> 中所有值都不同。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>第一反应是<strong>并查集 + gcd</strong>：<ul>
<li>以 <code>nums.size()</code> 作为并查集的大小，尝试连通 <code>nums</code> 中各个数值。</li>
<li>用 <code>gcd</code> 来求得两个数的最大公约数，判断结果是否大于 <code>1</code>。</li>
<li>但用这个方法进行模拟会超时，只通过了部分案例。【关键应该在于 <code>nums[i]</code> 的取值范围达到 $10^5$，假设是两个很大的质数，用来求 <code>gcd</code> 的过程就会很耗时间】</li>
</ul>
</li>
<li>官方题解中的思路也是利用并查集，但判断连通组件大小之前会将 <code>nums[i]</code> 与各自的因数先连通。<ul>
<li>以 <code>max(nums) + 1</code> 作为并查集的大小。</li>
<li>接着遍历 <code>nums</code> 数组，将 <code>nums[i]</code> 的因子与 <code>nums[i]</code> 进行连通。【将 <code>sqrt(nums[i])</code> 往下的因子进行连通即可（即进行 <code>union</code> 操作），因为因子是成对的，每次能将 <code>nums[i]</code> 分别与两个数连通】</li>
<li>最后就是遍历 <code>nums</code> 数组，在并查集中搜索 <code>nums[i]</code> 的 <code>root</code> 进行大小上的累加，过程中确认最大值即可。<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2></li>
</ul>
</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">DSU</span> <span class="comment">// 并查集/rank优化</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; parent;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; rank;</span><br><span class="line">    <span class="built_in">DSU</span>(<span class="type">int</span> N) : <span class="built_in">parent</span>(N), <span class="built_in">rank</span>(N)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; ++i) parent[i] = i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">int</span> _find(<span class="type">int</span> x) <span class="comment">// 找root</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(parent[x] != x) parent[x] = _find(parent[x]);</span><br><span class="line">        <span class="keyword">return</span> parent[x];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">void</span> _union(<span class="type">int</span> x, <span class="type">int</span> y)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// parent[_find(x)] = _find(y);</span></span><br><span class="line">        <span class="type">int</span> px = _find(x), py = _find(y);</span><br><span class="line">        <span class="keyword">if</span>(px == py) <span class="keyword">return</span>;</span><br><span class="line">        <span class="keyword">if</span>(rank[px] &lt; rank[py])</span><br><span class="line">            parent[px] = py;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(rank[px] &gt; rank[py])</span><br><span class="line">            parent[py] = px;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            parent[py] = px;</span><br><span class="line">            ++rank[px];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">largestComponentSize</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; nums)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="type">int</span> m = *<span class="built_in">max_element</span>(nums.<span class="built_in">begin</span>(), nums.<span class="built_in">end</span>());</span><br><span class="line">        <span class="function">DSU <span class="title">dsu</span><span class="params">(m+<span class="number">1</span>)</span></span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> num : nums) <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">2</span>; i * i &lt;= num; ++i)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(num % i == <span class="number">0</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                dsu._union(num, i);</span><br><span class="line">                dsu._union(num, num/i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">cnts</span><span class="params">(m+<span class="number">1</span>)</span></span>;</span><br><span class="line">        <span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> num : nums)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> p = dsu._find(num);</span><br><span class="line">            ++cnts[p];</span><br><span class="line">            ans = <span class="built_in">max</span>(ans, cnts[p]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/8/1】1374. 生成每种字符都是奇数个的字符串</title>
    <url>/p/d752fe72.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个整数 <code>n</code>，返回一个含 <code>n</code> 个字符的字符串，其中每种字符在字符串中都恰好出现奇数次。<ul>
<li>只含小写英文字母。</li>
<li>返回一种可能性即可。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>该题毫无意义。</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">string <span class="title">generateTheString</span><span class="params">(<span class="type">int</span> n)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        string ans = <span class="string">&quot;&quot;</span>;</span><br><span class="line">        <span class="keyword">if</span>(n % <span class="number">2</span> == <span class="number">0</span>) ans += <span class="string">&#x27;a&#x27;</span>, --n;</span><br><span class="line">        <span class="keyword">while</span>(n-- &gt; <span class="number">0</span>) ans += <span class="string">&#x27;b&#x27;</span>;</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/8/2】622. 设计循环队列</title>
    <url>/p/ca3dd10c.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>设计循环队列。<ul>
<li>循环队列是一种线性数据结构，其操作表现基于 FIFO（先进先出）原则并且队尾被连接在队首之后以形成一个循环。</li>
</ul>
</li>
<li>该实现类 <code>MyCircularQueue</code> 支持如下操作：<ul>
<li><code>MyCircularQueue(k)</code>：构造器，<code>k</code> 为队列的长度。</li>
<li><code>Front()</code>：从队首获取元素，如果队列为空，返回 <code>-1</code>。</li>
<li><code>Rear()</code>：获取队尾元素，如果队列为空，返回 <code>-1</code>。</li>
<li><code>enQueue(value)</code>：向循环队列插入一个元素，如果成功插入则返回真。</li>
<li><code>deQueue()</code>：从循环队列中删除一个元素，如果成功删除则返回真。</li>
<li><code>isEmpty()</code>：检查循环队列是否为空。</li>
<li><code>isFull()</code>：检查循环队列是否已满。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>初始的模拟思路很直观：<ul>
<li><code>MyCircularQueue</code> 中包含三个成员，<code>vec</code>（固定大小的数组）、 <code>head</code>（队列头位置索引）和 <code>tail</code>（队列尾位置索引）。【注意 <code>tail</code> 代表尾后位置】</li>
<li>初始化 <code>MyCircularQueue(k)</code> 中将 <code>vec</code> 置为大小为 <code>k</code> 的数组，再将 <code>head</code> 和 <code>tail</code> 均置为 <code>0</code>。</li>
<li>很自然地，初始化后 <code>head == tail == 0</code>，此时循环队列为空，则至少说明 <code>isEmpty()</code> 可以根据 <code>head</code> 和 <code>tail</code> 的值是否相等来返回结果。</li>
<li>向循环队列插入或删除操作也不难推导出来，插入操作在设置值的同时把 <code>tail</code> 后移一位，删除操作直接将 <code>head</code> 后移一位。【插入操作需要提前判断循环队列是否已满，删除操作需要提前判断循环队列是否为空】【<code>head</code> 的后移一位操作在遇到边界的时候需要重新回到索引 <code>0</code>，所以后移操作做一个取模操作即可，即 <code>(head+1)%sz</code>（其中 <code>sz</code> 为 <code>vec.size()</code>）】【<code>tail</code> 的后移操作同理】</li>
<li><code>Front()</code> 首先判断队列是否为空，不为空则返回 <code>head</code> 索引上的值。</li>
<li><code>rear()</code> 首先判断队列是否为空，不为空则返回 <code>(tail+sz-1)%sz</code> 索引上的值（其中 <code>sz</code> 为 <code>vec.size()</code>）。</li>
<li>到目前为止，这个思路的方向还算正确，但是在判断循环队列是否为满时会出现问题：<ul>
<li>如下图所示，循环队列在 <code>enQueue(3)</code> 后使得循环队列为满，但此时再次出现 <code>head == tail</code>：<img src="/p/ca3dd10c/leetcode_20220802.jpg" alt="leetcode_20220802"></li>
<li>这种情况说明如果按目前的假设， <code>isEmpty()</code> 的实现和 <code>isFull()</code> 的实现会产生矛盾。</li>
</ul>
</li>
</ul>
</li>
<li>为了避免判空和判满的矛盾，有两种方法：【两种方法均是延续原来的思路，对原来的数据结构进行补充】<ol>
<li>设置一个 <code>bool</code> 类型值 <code>flag</code>，<code>flag</code> 在 <code>enQueue(value)</code> 和 <code>deQueue()</code> 中维护更新，则判满操作就可以根据 <code>flag</code> 值的不同与判空操作进行区分。</li>
<li>在初始化中将 <code>vec</code> 设置为大小为 <code>k+1</code> 的数组，则判满操作可以设置为返回 <code>head == (tail+1)%capacity</code>，其他操作仍然和上述思路保持一致。【后面代码实现是依据方法二】</li>
</ol>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyCircularQueue</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; vec; <span class="comment">// vec在这里是固定大小数组，容量不会扩增</span></span><br><span class="line">    <span class="type">int</span> head;</span><br><span class="line">    <span class="type">int</span> tail;</span><br><span class="line">    <span class="type">int</span> capacity; <span class="comment">// 其实就是vec.size()</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyCircularQueue</span>(<span class="type">int</span> k) : <span class="built_in">vec</span>(k+<span class="number">1</span>), <span class="built_in">head</span>(<span class="number">0</span>), <span class="built_in">tail</span>(<span class="number">0</span>), <span class="built_in">capacity</span>(k+<span class="number">1</span>) &#123;&#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">enQueue</span><span class="params">(<span class="type">int</span> value)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">isFull</span>()) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        vec[tail%capacity] = value;</span><br><span class="line">        tail = (tail+<span class="number">1</span>)%capacity;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">deQueue</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">isEmpty</span>()) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        head = (head+<span class="number">1</span>)%capacity;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">Front</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">isEmpty</span>()) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">        <span class="keyword">return</span> vec[head];</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">Rear</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="built_in">isEmpty</span>()) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">        <span class="keyword">return</span> vec[(tail+capacity<span class="number">-1</span>)%capacity];</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isEmpty</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> head == tail;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="type">bool</span> <span class="title">isFull</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> head == (tail+<span class="number">1</span>)%capacity;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/8/3】899. 有序队列</title>
    <url>/p/5c95f812.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个字符串 <code>s</code> 和一个整数 <code>k</code>，可以从 <code>s</code> 的前 <code>k</code> 个字母中选择一个并把它加到字符串的末尾，经过多次前述操作后字符串可以达到字典序最小。<ul>
<li>返回多次操作后字典序最小的字符串。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>初看难度是困难，以为不是模拟题，看了多个实例，当 <code>k=1</code> 时情况其实只能通过模拟来判断。【<code>k=1</code> 时操作是确定的，每次只能移动队首元素】<ul>
<li>看了题解之后才意识到是脑筋急转弯，只要 <code>k!=1</code>，都能够通过有限的操作使得字符串转变成排序后的结果。</li>
<li>代码实现不难，难在证明该思路的准确性。</li>
</ul>
</li>
<li>官方题解也提及当 <code>k=1</code> 的时候可以通过 <code>O(n^2)</code> 的时间复杂度来模拟整个过程，也可以通过使用<strong>最小表示法</strong>来将时间复杂度降低到 <code>O(n)</code>。<ul>
<li>懒得去看了，直接模拟吧。</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">string <span class="title">orderlyQueue</span><span class="params">(string s, <span class="type">int</span> k)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(k &gt; <span class="number">1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">sort</span>(s.<span class="built_in">begin</span>(), s.<span class="built_in">end</span>());</span><br><span class="line">            <span class="keyword">return</span> s;</span><br><span class="line">        &#125;</span><br><span class="line">        string ans = s;</span><br><span class="line">        <span class="type">int</span> m = (<span class="type">int</span>)s.<span class="built_in">size</span>() - <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">while</span>(m-- &gt; <span class="number">0</span>) <span class="comment">// 执行 s.size()-1 次模拟</span></span><br><span class="line">        &#123;</span><br><span class="line">            s += s[<span class="number">0</span>]; <span class="comment">// 把队首元素添至队尾</span></span><br><span class="line">            s.<span class="built_in">erase</span>(<span class="number">0</span>, <span class="number">1</span>); <span class="comment">// erase(0, 1) 表示从位置 0 开始，earse 掉 1 个元素</span></span><br><span class="line">            ans = <span class="built_in">min</span>(s, ans);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/8/4】1403. 非递增顺序的最小子序列</title>
    <url>/p/13498852.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定数组 <code>nums</code>，从中抽取一个子序列并返回。<ul>
<li>返回的子序列满足：该子序列的元素之和严格大于未包含在该子序列中的各元素之和。</li>
<li>如果存在多个解决方案，只需返回<strong>长度最小</strong>的子序列。</li>
<li>如果仍然有多个解决方案，则返回<strong>元素之和最大</strong>的子序列。</li>
</ul>
</li>
<li>与子数组不同的地方在于，「数组的子序列」不强调元素在原数组中的连续性，也就是说，它可以通过从数组中分离一些（也可能不分离）元素得到。</li>
<li>注意，题目数据保证满足所有约束条件的解决方案是唯一的，同时返回的答案应当按非递增顺序排列。</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>贪心策略，排序后从最大值开始取即可。</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">minSubsequence</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; nums)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="type">int</span> sum = <span class="built_in">accumulate</span>(nums.<span class="built_in">begin</span>(), nums.<span class="built_in">end</span>(), <span class="number">0</span>);</span><br><span class="line">        <span class="built_in">sort</span>(nums.<span class="built_in">begin</span>(), nums.<span class="built_in">end</span>(), <span class="built_in">greater</span>&lt;<span class="type">int</span>&gt;());</span><br><span class="line">        <span class="type">int</span> cmp = <span class="number">0</span>;</span><br><span class="line">        vector&lt;<span class="type">int</span>&gt; ans;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> num : nums)</span><br><span class="line">        &#123;</span><br><span class="line">            cmp += num;</span><br><span class="line">            ans.<span class="built_in">push_back</span>(num);</span><br><span class="line">            <span class="keyword">if</span>(cmp &gt; sum / <span class="number">2</span>) <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/8/5】623. 在二叉树中增加一行</title>
    <url>/p/7dc3e81a.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个二叉树的根 <code>root</code> 和两个整数 <code>val</code> 和 <code>depth</code>，在给定的深度 <code>depth</code> 处添加一个值为 <code>val</code> 的节点行。<ul>
<li>根节点 <code>root</code> 位于深度 <code>1</code>。</li>
<li>给定整数 <code>depth</code>，对于深度为 <code>depth-1</code> 的每个非空树节点 <code>cur</code>，创建两个值为 <code>val</code> 的树节点作为 <code>cur</code> 的左子树根和右子树根。</li>
<li><code>cur</code> 原来的左子树应该是新的左子树根的左子树。</li>
<li><code>cur</code> 原来的右子树应该是新的右子树根的右子树。</li>
<li>如果 <code>depth == 1</code>，则创建一个新的树节点，原来的树根就是新根的左节点。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>二叉树基础应用题，dfs 和 bfs 解法均可以。<ul>
<li>在遍历过程中维护当前高度即可。</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">TreeNode* <span class="title">addOneRow</span><span class="params">(TreeNode* root, <span class="type">int</span> val, <span class="type">int</span> depth)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(depth == <span class="number">1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            TreeNode* head = <span class="keyword">new</span> <span class="built_in">TreeNode</span>(val);</span><br><span class="line">            head-&gt;left = root;</span><br><span class="line">            <span class="keyword">return</span> head;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">int</span> level = <span class="number">1</span>;</span><br><span class="line">        queue&lt;TreeNode*&gt; q;</span><br><span class="line">        q.<span class="built_in">push</span>(root);</span><br><span class="line">        <span class="keyword">while</span>(!q.<span class="built_in">empty</span>())</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(level == depth) <span class="keyword">break</span>;</span><br><span class="line">            <span class="type">int</span> sz = q.<span class="built_in">size</span>();</span><br><span class="line">            <span class="keyword">while</span>(sz-- &gt; <span class="number">0</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                TreeNode* cur = q.<span class="built_in">front</span>(); q.<span class="built_in">pop</span>();</span><br><span class="line">                <span class="keyword">if</span>(level+<span class="number">1</span> == depth)</span><br><span class="line">                &#123;</span><br><span class="line">                    TreeNode* l = cur-&gt;left;</span><br><span class="line">                    TreeNode* r = cur-&gt;right;</span><br><span class="line">                    cur-&gt;left = <span class="keyword">new</span> <span class="built_in">TreeNode</span>(val);</span><br><span class="line">                    cur-&gt;right = <span class="keyword">new</span> <span class="built_in">TreeNode</span>(val);</span><br><span class="line">                    cur-&gt;left-&gt;left = l;</span><br><span class="line">                    cur-&gt;right-&gt;right = r;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span>(cur-&gt;left) q.<span class="built_in">push</span>(cur-&gt;left);</span><br><span class="line">                <span class="keyword">if</span>(cur-&gt;right) q.<span class="built_in">push</span>(cur-&gt;right);</span><br><span class="line">            &#125;</span><br><span class="line">            ++level;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> root;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【2022/8/6】1408. 数组中的字符串匹配</title>
    <url>/p/b6ee6a48.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个字符串数组 <code>words</code>，数组中的每个字符串都可以看作是一个单词。<ul>
<li>按任意顺序返回 <code>words</code> 中是其他单词的子字符串的所有单词。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>暴力法解题，两重循环，判断内循环的字符串是否包含外循环的字符串。</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">vector&lt;string&gt; <span class="title">stringMatching</span><span class="params">(vector&lt;string&gt;&amp; words)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        vector&lt;string&gt; ans;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; words.<span class="built_in">size</span>(); ++i) <span class="keyword">for</span>(<span class="type">int</span> j = <span class="number">0</span>; j &lt; words.<span class="built_in">size</span>(); ++j)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(words[i].<span class="built_in">size</span>() &gt;= words[j].<span class="built_in">size</span>()) <span class="keyword">continue</span>; <span class="comment">// 排除了两个字符串相等的情况</span></span><br><span class="line">            <span class="keyword">if</span>(words[j].<span class="built_in">find</span>(words[i]) != string::npos)</span><br><span class="line">            &#123;</span><br><span class="line">                ans.<span class="built_in">push_back</span>(words[i]);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>leetcode每日一题</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【剑指offer Ⅱ】001. 整数除法</title>
    <url>/p/48dd08e5.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定两个整数 <code>a</code> 和 <code>b</code>，求它们的除法的商 <code>a/b</code>，要求不得使用乘号、除号以及求余符号。<ul>
<li>整数除法的结果应当截去其小数部分。【即向下取整】</li>
<li>假设环境只能存储 32 位有符号整数，其数值范围是 $[−2^{31}, 2^{31}−1]$。</li>
<li>如果除法结果溢出，则返回 $2^{31}−1$。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>最朴素的方法是很直观的，首先假设 <code>a</code> 和 <code>b</code> 均为正整数，一直使用除数 <code>a</code> 减去被除数 <code>b</code>，直到 <code>a &lt; b</code>，其中记录并更新进行减操作的次数，相当于以步进的方式更新商：<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> ans = <span class="number">0</span>; <span class="comment">// 商</span></span><br><span class="line"><span class="keyword">while</span>(a &gt;= b)</span><br><span class="line">&#123;</span><br><span class="line">  a -= b;</span><br><span class="line">  ++ans;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>但步进的实现只能通过部分案例，因为假若分子分母相差较大，则很容易超时，此时需要考虑如何加快搜索，而利用<strong>快速乘</strong>的思想（类似于<strong>快速幂</strong>）可以对算法进行优化。<ul>
<li>优化后不再采取步进的方式，而是通过<strong>倍增</strong>被除数 <code>b</code> 来找到商：<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> ans = <span class="number">0</span>; <span class="comment">// 最终的商</span></span><br><span class="line"><span class="keyword">while</span>(a &gt;= b)</span><br><span class="line">&#123;</span><br><span class="line">  <span class="type">int</span> tmp = b; <span class="comment">// 被除数需要倍增，所以用一个新的变量来进行更新</span></span><br><span class="line">  <span class="type">int</span> c = <span class="number">1</span>; <span class="comment">// 当前的商，用来倍增作记录，后续会更新到ans中去</span></span><br><span class="line">  <span class="keyword">while</span>(tmp+tmp &lt;= a)</span><br><span class="line">  &#123;</span><br><span class="line">    tmp += tmp; <span class="comment">// tmp &lt;&lt;= 1</span></span><br><span class="line">    c += c; <span class="comment">// c &lt;&lt;= 1</span></span><br><span class="line">  &#125;</span><br><span class="line">  a -= tmp;</span><br><span class="line">  ans += c;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li>目前的思路只考虑 <code>a</code> 和 <code>b</code> 均为正整数的情况，且没有考虑很多的溢出情况，后续只需要对所有可能溢出的地方进行处理即可。【但通常处理溢出都很繁琐】</li>
<li>后续代码实现需要注意的地方有：<ol>
<li>将 <code>a</code> 和 <code>b</code> 均置为负数，是因为 <code>INT_MIN</code> 的绝对值要比 <code>INT_MAX</code> 更大，因而为了避免将 <code>INT_MIN</code> 取反导致溢出，则将两个数置为负数，最后再将结果添上负号更方便。【取为负数后，前面的思路很多地方需要修改，如 <code>while(a &gt;= b)</code> 需要改为判断 <code>while(a &lt;= b)</code>】</li>
<li>在 while 循环中需要判断 <code>tmp &lt;= INT_MIN &gt;&gt; 1</code>，避免 <code>tmp + tmp</code> 溢出。</li>
<li>还要注意在代码中 <code>ans</code> 和 <code>c</code> 均为 <code>unsigned</code>，这是因为 leetcode 中带的 C++ 编译器不允许 int 溢出，如果溢出会直接报错。【这里倍增 <code>c</code> 时产生的溢出对于题目来说是允许的】</li>
</ol>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> INT_LIMIT = INT_MIN &gt;&gt; <span class="number">1</span>; <span class="comment">// -1073741824</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">divide</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(a == INT_MIN <span class="keyword">and</span> b == <span class="number">-1</span>) <span class="keyword">return</span> INT_MAX;</span><br><span class="line">        <span class="type">bool</span> neg = ((a &gt;&gt; <span class="number">31</span>) ^ (b &gt;&gt; <span class="number">31</span>)) != <span class="number">0</span>; <span class="comment">// 位运算确定符号</span></span><br><span class="line">        <span class="keyword">if</span>(a &gt; <span class="number">0</span>) a = -a;</span><br><span class="line">        <span class="keyword">if</span>(b &gt; <span class="number">0</span>) b = -b;</span><br><span class="line">        <span class="type">unsigned</span> ans = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span>(a &lt;= b)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> tmp = b; <span class="comment">// 当前除数</span></span><br><span class="line">            <span class="type">unsigned</span> c = <span class="number">1</span>; <span class="comment">// 当前商</span></span><br><span class="line">            <span class="keyword">while</span>(tmp &gt;= INT_LIMIT <span class="keyword">and</span> a &lt;= tmp + tmp) <span class="comment">// 快速乘法</span></span><br><span class="line">            &#123;</span><br><span class="line">                tmp += tmp;</span><br><span class="line">                c += c;</span><br><span class="line">            &#125;</span><br><span class="line">            a -= tmp; <span class="comment">// 求得a剩余部分</span></span><br><span class="line">            ans += c; <span class="comment">// 累计商</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> neg ? -ans : ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>剑指offer Ⅱ</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>剑指offer Ⅱ</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【剑指offer Ⅱ】002. 二进制加法</title>
    <url>/p/d4c26b3a.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定两个 01 字符串 <code>a</code> 和 <code>b</code>，计算它们的和并以二进制字符串的形式输出。<ul>
<li>输入字符串非空且只包含 <code>0</code> 和 <code>1</code>。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>该题是 leetcode 第 2 题两数相加的二进制字符串版本。</li>
<li>模拟进位加法即可，算法步骤如下：<ol>
<li>设置进位 <code>ca</code> 为 <code>0</code>，并初始化结果字符串 <code>ans</code> 为空字符串。</li>
<li>从后往前遍历两个字符串，只要有一个字符串未走完全程则继续遍历。<ol>
<li>遍历过程中，分别取得字符串 <code>a</code> 和 <code>b</code> 在各自索引的数值（如果字符串 <code>a</code> 或 <code>b</code> 已走完全程就直接将对应数值置为 <code>0</code>）。</li>
<li>将两个数值与进位 <code>ca</code> 进行相加，根据相加结果更新 <code>ans</code> 和 <code>ca</code>。</li>
</ol>
</li>
<li>遍历结束后，判断进位 <code>ca</code> 是否为 <code>1</code>，如果为 <code>1</code> 则需要更新 <code>ans</code>。</li>
<li>最后将 <code>ans</code> 进行 <code>reverse</code> 操作即可。</li>
</ol>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">string <span class="title">addBinary</span><span class="params">(string a, string b)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="type">int</span> m = a.<span class="built_in">size</span>()<span class="number">-1</span>, n = b.<span class="built_in">size</span>()<span class="number">-1</span>;</span><br><span class="line">        string ans = <span class="string">&quot;&quot;</span>;</span><br><span class="line">        <span class="type">int</span> ca = <span class="number">0</span>; <span class="comment">// 进位</span></span><br><span class="line">        <span class="keyword">while</span>(m &gt;= <span class="number">0</span> <span class="keyword">or</span> n &gt;= <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> num1 = m &gt;= <span class="number">0</span> ? a[m] - <span class="string">&#x27;0&#x27;</span> : <span class="number">0</span>;</span><br><span class="line">            <span class="type">int</span> num2 = n &gt;= <span class="number">0</span> ? b[n] - <span class="string">&#x27;0&#x27;</span> : <span class="number">0</span>;</span><br><span class="line">            ca += num1 + num2;</span><br><span class="line">            ans += ca &amp; <span class="number">1</span> == <span class="number">1</span> ? <span class="string">&#x27;1&#x27;</span> : <span class="string">&#x27;0&#x27;</span>;</span><br><span class="line">            ca /= <span class="number">2</span>;</span><br><span class="line">            --m, --n;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(ca == <span class="number">1</span>) ans += <span class="string">&#x27;1&#x27;</span>;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(ans == <span class="string">&quot;&quot;</span>) ans += <span class="string">&#x27;0&#x27;</span>;</span><br><span class="line">        <span class="built_in">reverse</span>(ans.<span class="built_in">begin</span>(), ans.<span class="built_in">end</span>());</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>剑指offer Ⅱ</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>剑指offer Ⅱ</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【剑指offer Ⅱ】003. 前 n 个数字二进制中 1 的个数</title>
    <url>/p/ce69a40a.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个非负整数 <code>n</code>，计算 <code>0</code> 到 <code>n</code> 之间的每个数字的二进制表示中 <code>1</code> 的个数，并输出一个数组。</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>该题考查位运算。<ul>
<li>假设要确定 <code>4</code> 的二进制表示中 <code>1</code> 的个数，可以通过确定 <code>2</code> 的二进制表示中 <code>1</code> 的个数，这是因为 <code>4 = 2 &lt;&lt; 1</code>，因而 <code>4</code> 和 <code>2</code> 的二进制表示中 <code>1</code> 的个数都是一样的。</li>
<li>而 <code>5</code> 可以分解成 <code>4 + 1</code>，即 <code>5 = 4 + 1 = 2 &lt;&lt; 1 + 1</code>。</li>
</ul>
</li>
<li>利用动态规划的解法来做即可，给定一个非负整数 <code>n</code>，则创建一个大小为 <code>n+1</code> 的数组 <code>ans</code> 用来记录每个数字的二进制表示中 <code>1</code> 的个数，而 <code>ans[i]</code> 的状态只由 <code>ans[i/2]</code> 确定。</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">countBits</span><span class="params">(<span class="type">int</span> n)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">ans</span><span class="params">(n+<span class="number">1</span>)</span></span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt;= n; ++i)</span><br><span class="line">            ans[i] = ans[i/<span class="number">2</span>] + (i&amp;<span class="number">1</span>);</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>剑指offer Ⅱ</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>剑指offer Ⅱ</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【剑指offer Ⅱ】004. 只出现一次的数字</title>
    <url>/p/b2c7e28e.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个整数数组 <code>nums</code>，除某个元素仅出现一次外，其余每个元素都恰出现三次，找出并返回那个只出现了一次的元素。</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>该题仍然是考察位运算，但并非利用异或（如果题目中的要求提及其他元素恰出现两次，则可以利用异或操作来解题）。<ul>
<li>把每个整数看成二进制表达形式，当同一个数出现三次，则可以看作将对应位上的 <code>0</code> 或 <code>1</code> 出现三次。</li>
<li>遍历每个数值，将数值对应的 32 位二进制出现的 <code>1</code> 出现的次数进行<strong>对应位置上的累加</strong>（累加到一个 32 位数组中进行保存）。</li>
<li>遍历完成后从 32 位的数组中取出每一位上的数值，并将每一位进行 <code>mod 3</code> 操作然后提取出对应位上的 <code>0</code> 或 <code>1</code> 即可。</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">singleNumber</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; nums)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">cnt</span><span class="params">(<span class="number">32</span>)</span></span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; <span class="number">32</span>; ++i)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> num : nums)</span><br><span class="line">                cnt[i] += (num &gt;&gt; i) &amp; <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">31</span>; i &gt;= <span class="number">0</span>; --i)</span><br><span class="line">            ans = (ans &lt;&lt; <span class="number">1</span>) + (cnt[i] % <span class="number">3</span>);</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>剑指offer Ⅱ</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>剑指offer Ⅱ</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【剑指offer Ⅱ】005. 单词长度的最大乘积</title>
    <url>/p/6995272.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个字符串数组 <code>words</code>，请计算当两个字符串 <code>words[i]</code> 和 <code>words[j]</code> 不包含相同字符时，它们长度的乘积的最大值。<ul>
<li>如果没有不包含相同字符的一对字符串，返回 <code>0</code>。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>该题仍然是考察位运算。<ul>
<li>英文字母 26 位，而一个 int 数值占 32 位，所以可以考虑用一个 int 数值保存一个单词中英文字母出现的情况。<ul>
<li>例如单词中出现字母 <code>a</code>，则数值中的第 <code>0</code> 位置 <code>1</code>。</li>
</ul>
</li>
<li>将单词进行位掩码表示，每个单词对应一个位掩码。<ul>
<li>如此要判断两个字符串是否包含相同字符，只需要判断<strong>对应的位掩码做与运算的结果是否为</strong> <code>0</code>即可。</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">maxProduct</span><span class="params">(vector&lt;string&gt;&amp; words)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="type">int</span> m = words.<span class="built_in">size</span>();</span><br><span class="line">        <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">nums</span><span class="params">(m)</span></span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; m; ++i)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">char</span> c : words[i])</span><br><span class="line">                nums[i] |= (<span class="number">1</span> &lt;&lt; (c - <span class="string">&#x27;a&#x27;</span>));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">int</span> ans = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; m<span class="number">-1</span>; ++i) <span class="keyword">for</span>(<span class="type">int</span> j = i + <span class="number">1</span>; j &lt; m; ++j)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>((nums[i] &amp; nums[j]) == <span class="number">0</span>)</span><br><span class="line">                ans = <span class="built_in">max</span>(ans, <span class="built_in">int</span>(words[i].<span class="built_in">size</span>() * words[j].<span class="built_in">size</span>()));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>剑指offer Ⅱ</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>剑指offer Ⅱ</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【剑指offer Ⅱ】006. 排序数组中两个数字之和</title>
    <url>/p/d20b5947.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>排序数组中有两个数字之和等于 <code>target</code>，返回这两个数的索引。</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>双指针基础应用题，套用框架即可。</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">twoSum</span><span class="params">(vector&lt;<span class="type">int</span>&gt;&amp; numbers, <span class="type">int</span> target)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="type">int</span> i = <span class="number">0</span>, j = numbers.<span class="built_in">size</span>()<span class="number">-1</span>;</span><br><span class="line">        <span class="keyword">while</span>(i &lt; j)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> sum = numbers[i] + numbers[j];</span><br><span class="line">            <span class="keyword">if</span>(sum == target) <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">if</span>(sum &gt; target) --j;</span><br><span class="line">            <span class="keyword">else</span> ++i;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> &#123;i, j&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>剑指offer Ⅱ</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>剑指offer Ⅱ</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【剑指offer Ⅱ】007. 数组中和为 0 的三个数</title>
    <url>/p/3be58721.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个整数数组，返回和为 <code>0</code> 的所有不重复三元组。</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>该题是三数之和问题。</li>
<li>仍然是双指针的基础应用题，首先要<strong>排序数组</strong>，接着固定一个位置，用双指针解决剩下两个数的两数之和问题。</li>
<li>关键在于结果的去重：<ul>
<li>第一步去重在外层循环中 <code>i</code> 的遍历，如果 <code>nums[i] == nums[i-1]</code>，则直接跳过。【此步去重生效是因为已经将数组排序】</li>
<li>第二步去重在三元组满足条件时，需要同时移动双指针 <code>j</code> 和 <code>k</code>，此时就需要将 <code>j</code> 指针跳过其右侧所有相等的数，还需要将 <code>k</code> 指针跳过其左侧所有相等的数。</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">threeSum</span>(vector&lt;<span class="type">int</span>&gt;&amp; nums)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> m = nums.<span class="built_in">size</span>();</span><br><span class="line">        <span class="keyword">if</span>(m &lt;= <span class="number">2</span>) <span class="keyword">return</span> &#123;&#125;;</span><br><span class="line">        vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; ss;</span><br><span class="line">        <span class="built_in">sort</span>(nums.<span class="built_in">begin</span>(), nums.<span class="built_in">end</span>());</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; m<span class="number">-2</span>; ++i)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span>(i &gt; <span class="number">0</span> <span class="keyword">and</span> nums[i] == nums[i<span class="number">-1</span>]) <span class="keyword">continue</span>; <span class="comment">// 去重</span></span><br><span class="line">            <span class="type">int</span> j = i+<span class="number">1</span>, k = m<span class="number">-1</span>;</span><br><span class="line">            <span class="type">int</span> target = -nums[i];</span><br><span class="line">            <span class="keyword">while</span>(j &lt; k)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="type">int</span> sum = nums[j] + nums[k];</span><br><span class="line">                <span class="keyword">if</span>(sum == target) <span class="comment">// 注意去重</span></span><br><span class="line">                &#123;</span><br><span class="line">                    ss.<span class="built_in">push_back</span>(&#123;nums[i], nums[j], nums[k]&#125;);</span><br><span class="line">                    <span class="keyword">while</span>(j &lt; k <span class="keyword">and</span> nums[j] == nums[j+<span class="number">1</span>]) ++j;</span><br><span class="line">                    <span class="keyword">while</span>(j &lt; k <span class="keyword">and</span> nums[k] == nums[k<span class="number">-1</span>]) --k;</span><br><span class="line">                    ++j, --k;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span>(sum &gt; target) --k;</span><br><span class="line">                <span class="keyword">else</span> ++j;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ss;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>剑指offer Ⅱ</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>剑指offer Ⅱ</tag>
      </tags>
  </entry>
  <entry>
    <title>【leetcode】【剑指offer Ⅱ】008. 和大于等于 target 的最短子数组</title>
    <url>/p/e3f4b6f2.html</url>
    <content><![CDATA[<h2 id="问题描述："><a href="#问题描述：" class="headerlink" title="问题描述："></a>问题描述：</h2><ul>
<li>给定一个正整数数组和一个正整数 <code>target</code>。<ul>
<li>找出数组中满足和 <code>&gt;= target</code> 且长度最小的连续子数组，并返回其长度。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>滑动窗口基础应用题。<ul>
<li>扩大窗口直到满足条件（子数组和 <code>&gt;= target</code>），一旦满足条件就缩小窗口。</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">minSubArrayLen</span><span class="params">(<span class="type">int</span> target, vector&lt;<span class="type">int</span>&gt;&amp; nums)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="type">int</span> m = nums.<span class="built_in">size</span>();</span><br><span class="line">        <span class="type">int</span> ans = INT_MAX;</span><br><span class="line">        <span class="type">int</span> l = <span class="number">0</span>, r = <span class="number">0</span>;</span><br><span class="line">        <span class="type">int</span> sum = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span>(r &lt; m)</span><br><span class="line">        &#123;</span><br><span class="line">            sum += nums[r++];</span><br><span class="line">            <span class="keyword">while</span>(sum &gt;= target)</span><br><span class="line">            &#123;</span><br><span class="line">                ans = <span class="built_in">min</span>(ans, r-l);</span><br><span class="line">                sum -= nums[l++];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans == INT_MAX ? <span class="number">0</span> : ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>刷题</category>
        <category>剑指offer Ⅱ</category>
      </categories>
      <tags>
        <tag>leetcode</tag>
        <tag>剑指offer Ⅱ</tag>
      </tags>
  </entry>
  <entry>
    <title>【数据结构】并查集</title>
    <url>/p/2e7813af.html</url>
    <content><![CDATA[<h2 id="相关概念："><a href="#相关概念：" class="headerlink" title="相关概念："></a>相关概念：</h2><ul>
<li><strong>并查集</strong>是一种树形的数据结构，用于处理集合相关的问题。<ul>
<li>通常集合中的元素都不具备层次关系，而是因为共通的性质而被划分在一起。</li>
<li>当存在多个集合，而又需要判断任意两个元素是否处在同一个集合中时，就需要使用并查集来维护集合元素之间的关系。</li>
</ul>
</li>
<li><strong>并查集</strong>有两个主要的操作：<ol>
<li><strong>合并</strong>操作：将两个子集合并成一个子集。</li>
<li><strong>查找</strong>操作：确定某个元素处在哪个集合。</li>
</ol>
</li>
<li>一般用固定大小的数组来实现并查集，但可以用森林的模型来解释并查集的操作。【森林即多棵树】<ul>
<li>假设题目中元素只有 <code>m</code> 个，则并查集底层只需维护一个 <code>m</code> 大小的数组 <code>parent</code> 即可。【<code>parent</code> 数组保存对应元素的父节点】</li>
<li>对于合并操作的解释：将两个元素合并在同一个子集，就相当于<strong>将两棵树合并成一棵树</strong>。</li>
<li>对于查找操作的解释：要确定两个元素处于同一个集合只需要<strong>确定两个元素是否处于同一棵树</strong>即可。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>并查集的实现非常简洁。<ul>
<li>初始化：设置每个元素为一个集合，即设置每个元素的父节点为自己。<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; parent.<span class="built_in">size</span>(); ++i)</span><br><span class="line">  parent[i] = i;</span><br></pre></td></tr></table></figure></li>
<li>查找：找到元素所在集合的根，即所在树的根。【从代码中看出，<code>find</code> 函数会递归调用，直到找到集合的根】【此处的 <code>find</code> 函数已经进行<strong>路径压缩</strong>，简单来说，就是<strong>把当前搜索路径上的所有元素都连接到最顶部的根上去</strong>，这样每一棵树的高度都不会过分高】<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span> <span class="comment">// 找root</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(parent[x] != x) parent[x] = <span class="built_in">find</span>(parent[x]); <span class="comment">// 路径压缩，此处可以用 while 语句来实现路径压缩的非递归版本</span></span><br><span class="line">    <span class="keyword">return</span> parent[x];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>合并：分别找出两个元素的根进行连接即可。<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">union</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    parent[<span class="built_in">find</span>(x)] = <span class="built_in">find</span>(y);</span><br><span class="line">    <span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
</li>
<li>可以通过下述例子来加深理解。<ol>
<li>假设有 <code>5</code> 个元素，则初始化一个大小为 <code>5</code> 的 <code>parent</code> 数组：<img src="/p/2e7813af/union_find.jpg" alt="union_find"></li>
<li>假设元素 <code>0</code> 和元素 <code>1</code> 有关系，则调用 <code>union(0, 1)</code>，注意此时 <code>find(0)</code> 或 <code>find(1)</code> 都是简单地返回 <code>0</code> 或 <code>1</code>：<img src="/p/2e7813af/union_find_2.jpg" alt="union_find_2"></li>
<li>假设元素 <code>0</code> 分别和元素 <code>2</code> 、元素 <code>3</code> 有关系，则分别调用 <code>union(2, 0)</code> 和 <code>union(3, 0)</code>，注意 <code>find(0)</code> 会返回 <code>1</code>：<img src="/p/2e7813af/union_find_3.jpg" alt="union_find_3"></li>
</ol>
</li>
<li>最后要提及的是关于并查集的一些<strong>优化</strong>：<ul>
<li>可以注意到，上面例子的最后结果中留下了两棵树（即两个集合），其中 <code>root 1</code> 体型较大，<code>root 4</code> 体型较小，如果后续调用 <code>union(1, 4)</code> 则会把大树放置到小树底下（这样显然是不够平衡的），然而调用 <code>union(4, 1)</code> 就会避免前面的情况。【这样说明了不同的参数调用顺序会给 <code>union</code> 带来不同的结果】</li>
<li>有两种优化思路，一种是 <strong>with size</strong>，一种是 <strong>with rank</strong>。</li>
<li>简单来说，<strong>with size</strong> 方法就是并查集中维护一个 <code>size</code> 数组（记录元素所在树的大小），在 <code>union</code> 操作中进行更新；同理，<strong>with rank</strong> 方法就是维护一个 <code>rank</code> 数组（记录元素所在树的高度），同样是在 <code>union</code> 操作中进行更新。</li>
</ul>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><ul>
<li><strong>并查集</strong>实现如下：<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">DSU</span> <span class="comment">// 并查集</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; parent;</span><br><span class="line">    <span class="built_in">DSU</span>(<span class="type">int</span> N) : <span class="built_in">parent</span>(N)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// parent = vector&lt;int&gt;(N);</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; ++i) parent[i] = i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span> <span class="comment">// 找root</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(parent[x] != x) parent[x] = <span class="built_in">find</span>(parent[x]);</span><br><span class="line">        <span class="keyword">return</span> parent[x];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">void</span> _union(<span class="type">int</span> x, <span class="type">int</span> y)</span><br><span class="line">    &#123;</span><br><span class="line">        parent[<span class="built_in">find</span>(x)] = <span class="built_in">find</span>(y);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
<li><strong>并查集 with size</strong> 实现如下：<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">DSU</span> <span class="comment">// 并查集 with size</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; parent;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; size;</span><br><span class="line">    <span class="built_in">DSU</span>(<span class="type">int</span> N) : <span class="built_in">max_size</span>(<span class="number">1</span>), <span class="built_in">parent</span>(N), <span class="built_in">size</span>(N, <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; ++i) parent[i] = i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span> <span class="comment">// 找root</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(parent[x] != x) parent[x] = <span class="built_in">find</span>(parent[x]);</span><br><span class="line">        <span class="keyword">return</span> parent[x];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">union</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="comment">// parent[find(x)] = find(y);</span></span><br><span class="line">        <span class="type">int</span> px = <span class="built_in">find</span>(x), py = <span class="built_in">find</span>(y);</span><br><span class="line">        <span class="keyword">if</span>(px == py) <span class="keyword">return</span>;</span><br><span class="line">        <span class="keyword">if</span>(size[px] &lt;= size[py])</span><br><span class="line">        &#123;</span><br><span class="line">            parent[px] = py;</span><br><span class="line">            size[py] += size[px];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            parent[py] = px;</span><br><span class="line">            size[px] += size[py];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li>
<li><strong>并查集 with rank</strong> 实现如下：<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">DSU</span> <span class="comment">// 并查集 with rank</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; parent;</span><br><span class="line">    vector&lt;<span class="type">int</span>&gt; rank;</span><br><span class="line">    <span class="built_in">DSU</span>(<span class="type">int</span> N) : <span class="built_in">parent</span>(N), <span class="built_in">rank</span>(N)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i = <span class="number">0</span>; i &lt; N; ++i) parent[i] = i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">int</span> x)</span> <span class="comment">// 找root</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(parent[x] != x) parent[x] = <span class="built_in">find</span>(parent[x]);</span><br><span class="line">        <span class="keyword">return</span> parent[x];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="type">void</span> _union(<span class="type">int</span> x, <span class="type">int</span> y)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> px = <span class="built_in">find</span>(x), py = <span class="built_in">find</span>(y);</span><br><span class="line">        <span class="keyword">if</span>(px == py) <span class="keyword">return</span>;</span><br><span class="line">        <span class="keyword">if</span>(rank[px] &lt; rank[py])</span><br><span class="line">            parent[px] = py;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(rank[px] &gt; rank[py])</span><br><span class="line">            parent[py] = px;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            parent[py] = px;</span><br><span class="line">            ++rank[px];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h2 id="部分题目："><a href="#部分题目：" class="headerlink" title="部分题目："></a>部分题目：</h2><ul>
<li>leetcode - 305.岛屿数量 Ⅱ（会员题）</li>
<li>leetcode - 547.省份数量</li>
<li>leetcode - 128.最长连续序列</li>
<li>luogu - P1551.亲戚</li>
<li>luogu - P1536.村村通</li>
</ul>
]]></content>
      <categories>
        <category>数据结构</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>【图论】拓扑排序</title>
    <url>/p/fb40efc5.html</url>
    <content><![CDATA[<h2 id="相关概念："><a href="#相关概念：" class="headerlink" title="相关概念："></a>相关概念：</h2><ul>
<li><strong>拓扑排序</strong>，就是在有向图中找到一条拓扑序列，序列中排列在前的节点相较于排列在后的节点有更高的优先级。<ul>
<li>首先要理解优先级，优先级是有向图中边的方向性带来的结果，如 <code>a -&gt; b</code> 表示节点 <code>a</code> 指向节点 <code>b</code>，则可以认为 <code>a</code> 的优先级优于 <code>b</code>，在特定语境下可以说 <code>a</code> 必须在 <code>b</code> 之前完成，如下图所示，该有向图的拓扑排序结果为 <code>14325</code> 或 <code>41325</code>。<img src="/p/fb40efc5/leetcode_20220728.jpg" alt="topo_sort"></li>
<li>拓扑排序算法可以用来判断有向图是否有环，可以生成拓扑序列。<ul>
<li>网络中可能存在多个拓扑排序结果。</li>
<li>如果网络中不存在环，则结果中会包括所有节点。</li>
<li>但如果网络中存在环，则结果中可能会缺少某些节点。</li>
</ul>
</li>
</ul>
</li>
<li>图可以有不同的表现形式，如邻接矩阵表示形式和邻接表表示形式，有向图的<strong>邻接表</strong>表示形式。<ul>
<li>简单来说，假设图中有 <code>m</code> 个节点，邻接表就是维护一个 <code>m</code> 大小的数组 <code>vec</code>，其中数组元素为当前节点的外接节点链表。【例如在网络中 <code>a -&gt; b</code> 且 <code>a -&gt; c</code>，则数组中 <code>a</code> 节点对应的位置就保存一个链表 <code>b =&gt; c</code>，相当于 <code>vec(a) =&gt; b =&gt; c</code> （此处用 <code>=&gt;</code> 表示链表指针，<code>b</code> 和 <code>c</code> 在链表中的顺序不分先后）】</li>
<li>也就是说，数组中第 <code>i</code> 个元素（记住数组元素为链表）保存的是网络中第 <code>i</code> 个节点的 one-hop neighbor。【即节点跳一步就可以达到的邻居节点】</li>
</ul>
</li>
<li>进行后续算法的介绍前，还需要理解<strong>节点的入度</strong>。<ul>
<li>当前节点的入度，指的就是网络中有多少节点直接指向当前节点。【如上图所示，节点 <code>3</code> 的入度为 <code>2</code> 】</li>
<li>可用一个数组统计每个节点的入度。</li>
</ul>
</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>拓扑排序问题有两种解决方案：<strong>Kahn 算法</strong>和 <strong>dfs 着色解法</strong>。</li>
<li><strong>Kahn 算法</strong>的核心思想就是每次删除一个当前入度为 <code>0</code> 的节点，并删除该节点对链出邻居的影响（即其指向邻居的入度数减一）。<ul>
<li>Kahn 算法需要图的邻接表形式。</li>
<li>可以用 <code>stack</code> 或 <code>queue</code> 来保存当前入度为 <code>0</code> 的节点。【即用 <code>stack</code> 或 <code>queue</code> 维护当前入度为 <code>0</code> 的点集】</li>
<li>用上面的图来理解，Kahn 算法的第一步就是把节点 <code>1</code> 带来的影响消去，留下其余的节点和边作为子图进行拓扑排序：<img src="/p/fb40efc5/leetcode_20220728_2.jpg" alt="topo_sort_kahn"></li>
</ul>
</li>
<li><strong>dfs 着色解法</strong>即利用深度优先算法的方法以任意顺序遍历图中所有节点，但<strong>遍历的过程中需要对节点着色</strong>。<ul>
<li>不同的颜色代表访问的程度深浅，过程中遇到不同颜色的节点会有相应的处理；需要一个数组维护网络中节点的颜色，颜色的状态可以为 <code>0</code>、<code>-1</code> 和 <code>1</code>。<ul>
<li><code>-1</code> 表示访问过，如果重复遇上，则说明存在环。</li>
</ul>
</li>
<li>最后需要把结果数组进行 <code>reverse</code> 操作，得出来的才是拓扑序列。【因为 dfs 着色解法会优先深度搜索到优先级最低的节点，所以最先存入结果数组的就是优先级最低的节点，最后结果数组就需要翻转过来才可】</li>
</ul>
</li>
</ul>
<h2 id="算法步骤："><a href="#算法步骤：" class="headerlink" title="算法步骤："></a>算法步骤：</h2><ul>
<li><p><strong>Kahn 算法</strong>的算法步骤如下：【假设用栈来维护点集】</p>
<ol>
<li>初始化，所有入度为 <code>0</code> 的节点入栈。</li>
<li>每次从栈中 <code>pop</code> 出一个节点放入结果数组。<ol>
<li>同时该节点指向的所有节点都需要进行入度减一操作。</li>
<li>如果邻居的入度变为 <code>0</code> 则邻居入栈。</li>
</ol>
</li>
<li>不断重复 2 过程直到栈为空。</li>
<li>如果结果数组个数为网络中节点个数，则说明网络中无环，否则说明网络中有环。</li>
</ol>
</li>
<li><p><strong>dfs 着色解法</strong>的算法步骤如下：</p>
<ol>
<li>初始化，所有点染色为 <code>0</code>。</li>
<li>遍历每个节点进入 <code>dfs</code> 函数。【如果要遍历的点颜色不为 <code>0</code>，则不进入递归】<ol>
<li>假设当前节点为 <code>x</code>，将其染色为 <code>-1</code>，接着遍历 <code>x</code> 的邻居。</li>
<li>假若当前邻居 <code>y</code> 的颜色为 <code>0</code> ，则递归进入 <code>dfs(y)</code>；假若当前邻居 <code>y</code> 的颜色为 <code>-1</code>，则说明网络中存在环。</li>
<li>假若当前节点 <code>x</code> 的所有邻居都走完且没有发现环，则把 <code>x</code> 保存进结果数组中。</li>
</ol>
</li>
<li>将结果数组进行翻转操作。</li>
</ol>
</li>
</ul>
<h2 id="实现代码："><a href="#实现代码：" class="headerlink" title="实现代码："></a>实现代码：</h2><ul>
<li><p><strong>Kahn 算法</strong>的实现代码如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stack&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">kahn_algorithm</span><span class="params">(vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; &amp;graph, vector&lt;<span class="type">int</span>&gt; &amp;in, <span class="type">int</span> n)</span> </span>&#123;</span><br><span class="line">  vector&lt;<span class="type">int</span>&gt; ans;</span><br><span class="line">  stack&lt;<span class="type">int</span>&gt; stk;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 第一步，初始化</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">1</span>; i &lt;= n; ++i)</span><br><span class="line">    <span class="keyword">if</span> (in[i] == <span class="number">0</span>)</span><br><span class="line">      stk.<span class="built_in">push</span>(i);</span><br><span class="line">  </span><br><span class="line">  <span class="comment">// 第二步，从栈中一直 pop 出元素并存放到结果中</span></span><br><span class="line">  <span class="keyword">while</span> (!stk.<span class="built_in">empty</span>()) &#123;</span><br><span class="line">    <span class="type">int</span> x = stk.<span class="built_in">top</span>();</span><br><span class="line">    stk.<span class="built_in">pop</span>();</span><br><span class="line">    ans.<span class="built_in">push_back</span>(x);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;next : graph[x])</span><br><span class="line">      <span class="keyword">if</span> (--in[next] == <span class="number">0</span>)</span><br><span class="line">        stk.<span class="built_in">push</span>(next);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> ans;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="type">int</span> m, n; <span class="comment">// 输入 m 条边，n 个节点，节点标记为 1~n</span></span><br><span class="line">  cin &gt;&gt; m &gt;&gt; n;</span><br><span class="line">  <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">in</span><span class="params">(n + <span class="number">1</span>)</span></span>;            <span class="comment">// 入度数组</span></span><br><span class="line">  vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">graph</span>(n + <span class="number">1</span>); <span class="comment">// 邻接表</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; m; ++i)       <span class="comment">// 输入 m 的边的具体情况</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="type">int</span> a, b;</span><br><span class="line">    cin &gt;&gt; a &gt;&gt; b;</span><br><span class="line">    ++in[b];</span><br><span class="line">    graph[a].<span class="built_in">push_back</span>(b);</span><br><span class="line">  &#125;</span><br><span class="line">  vector&lt;<span class="type">int</span>&gt; ans = <span class="built_in">kahn_algorithm</span>(graph, in, n);</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> num : ans)</span><br><span class="line">    cout &lt;&lt; num &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">  cout &lt;&lt; endl;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>dfs 着色解法</strong>的实现代码如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line">vector&lt;<span class="type">int</span>&gt; ans;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">dfs</span><span class="params">(vector&lt;vector&lt;<span class="type">int</span>&gt;&gt;&amp; graph, vector&lt;<span class="type">int</span>&gt;&amp; color, <span class="type">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  color[x] = <span class="number">-1</span>;</span><br><span class="line">  <span class="keyword">for</span>(<span class="type">int</span> y : graph[x])</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">if</span>(color[y] == <span class="number">-1</span> <span class="built_in">or</span> (color[y] == <span class="number">0</span> <span class="keyword">and</span> <span class="built_in">dfs</span>(graph, color, y) == <span class="literal">false</span>)) <span class="comment">// 有环</span></span><br><span class="line">      <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  color[x] = <span class="number">1</span>;</span><br><span class="line">  ans.<span class="built_in">push_back</span>(x);</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">dfs_color</span><span class="params">(vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; &amp;graph, <span class="type">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">color</span><span class="params">(n+<span class="number">1</span>, <span class="number">0</span>)</span></span>;</span><br><span class="line">  <span class="keyword">for</span>(<span class="type">int</span> x = <span class="number">1</span>; x &lt;= n; ++x) <span class="keyword">if</span>(color[x] == <span class="number">0</span>)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">dfs</span>(graph, color, x) == <span class="literal">false</span>) <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">reverse</span>(ans.<span class="built_in">begin</span>(), ans.<span class="built_in">end</span>());</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="type">int</span> m, n; <span class="comment">// 输入 m 条边，n 个节点，节点标记为 1~n</span></span><br><span class="line">  cin &gt;&gt; m &gt;&gt; n;</span><br><span class="line">  vector&lt;vector&lt;<span class="type">int</span>&gt;&gt; <span class="built_in">graph</span>(n + <span class="number">1</span>); <span class="comment">// 邻接表</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; m; ++i)       <span class="comment">// 输入 m 条边的具体情况</span></span><br><span class="line">  &#123;</span><br><span class="line">    <span class="type">int</span> a, b;</span><br><span class="line">    cin &gt;&gt; a &gt;&gt; b;</span><br><span class="line">    graph[a].<span class="built_in">push_back</span>(b);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span>(<span class="built_in">dfs_color</span>(graph, n) == <span class="literal">false</span>)</span><br><span class="line">  &#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;有环&quot;</span> &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> num : ans)</span><br><span class="line">    cout &lt;&lt; num &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">  cout &lt;&lt; endl;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h2 id="部分题目："><a href="#部分题目：" class="headerlink" title="部分题目："></a>部分题目：</h2><ul>
<li>leetcode - 剑指 Offer Ⅱ 115.重建序列</li>
</ul>
]]></content>
      <categories>
        <category>算法</category>
        <category>图论</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>【字符串】最小表示法</title>
    <url>/p/840c62d6.html</url>
    <content><![CDATA[<h2 id="相关概念："><a href="#相关概念：" class="headerlink" title="相关概念："></a>相关概念：</h2><ul>
<li>在介绍后续算法前，首先要理解<strong>循环同构</strong>的概念。<ul>
<li>定义太拗口了，可以直接看例子：字符串 <code>abcd</code> 的循环同构有 <code>bcda</code>、<code>cdab</code> 和 <code>dabc</code>。</li>
</ul>
</li>
<li><strong>最小表示法</strong>是用来解决<strong>字符串最小表示问题</strong>的方法。<ul>
<li>而字符串的最小表示指的是字符串和它的<strong>所有循环同构中</strong>字典序最小的字符串。</li>
</ul>
</li>
<li>算法最后只需要返回循环同构中字典序最小的字符串的起始位置即可。</li>
</ul>
<h2 id="核心思路："><a href="#核心思路：" class="headerlink" title="核心思路："></a>核心思路：</h2><ul>
<li>可以把字符串看作是可循环的字符串，通常循环串的题目，都可以对循环串进行相应的处理：<strong>复制一倍</strong>。<ul>
<li>如字符串 <code>abcd</code> 复制一倍即变成 <code>abcdabcd</code>，如此在新的字符串中就可以访问到原字符串的所有循环同构。</li>
<li>实际代码中不需要真的开辟两倍的数组空间，只需要通过取模运算进行访问数组即可。【假设访问循环串 <code>S</code> 的当前索引为 <code>i</code>，而 <code>i &gt; n</code>，其中 <code>n</code> 为字符串大小，则可以通过 <code>S[i%n]</code> 来访问到对应字符】</li>
</ul>
</li>
<li>最小表示法的<strong>暴力解法</strong>很直观，因为已经进行复制一倍的操作，所以循环字符串的所有可能性都可以线性访问。<ul>
<li>则可以维护三个变量 <code>i</code>、<code>j</code> 和 <code>k</code>，<code>i</code> 和 <code>j</code> 表示两个字符串的起始索引，<code>k</code> 表示两个字符串同步访问的距离。</li>
<li>暴力解法只需要以 <code>O(n)</code> 的时间复杂度遍历所有的两对字符串，再在每一次遍历中以 <code>O(n)</code> 的时间复杂度对比两个字符串的字典序，对比完成后保留字典序更小的字符串起始位置即可（另一个位置向后移动）。</li>
</ul>
</li>
<li>可以从具体例子中理解暴力法的遍历过程：<ol>
<li>假设字符串 <code>s</code> 为 <code>abcd</code>，字符串长度 <code>n = 4</code>，且初始化 <code>i = 0</code>、<code>j = 1</code> 和 <code>k = 0</code>：<br><img src="/p/840c62d6/最小表示法.jpg" alt="最小表示法"></li>
<li>移动 <code>k</code> 之后，此时 <code>i = 0</code>、<code>j = 1</code> 和 <code>k = 1</code>：<br><img src="/p/840c62d6/最小表示法_2.jpg" alt="最小表示法_2"></li>
<li>在上一步的比较结束后，<strong>保留字典序更小的字符串的起始位置</strong> <code>i</code>，而将第二个字符串的起始位置 <code>j</code> 向后移动，此时需要进行下一次比较，因而重置 <code>k = 0</code>：<br><img src="/p/840c62d6/最小表示法_3.jpg" alt="最小表示法_3"></li>
<li>后续的遍历同理，所有遍历完之后返回 <code>min(i, j)</code> 即可。</li>
</ol>
</li>
<li>上述暴力解法其实是 <code>O(n^2)</code> 的时间复杂度，但是整个过程中有些比较可以避免，经过优化可以使得时间复杂度降为 <code>O(n)</code>：<ul>
<li>考虑以下的情况，字符串 <code>s</code> 为 <code>acacab</code>，且 <code>i = 0</code>、<code>j = 2</code> 和 <code>k = 3</code>，在本次比较完成后对 <code>i</code> 更新为 <code>i + k + 1</code> 以进行下一次比较（而在暴力法中会将 <code>i</code> 更新为 <code>i + 1</code>）：<br><img src="/p/840c62d6/最小表示法_4.jpg" alt="最小表示法_4"></li>
<li>优化的关键在于此：当以 <code>i</code> 为起始位置字符串的字典序更大时，会将 <code>i</code> 更新为 <code>i + k + 1</code>，因为所有以 <code>i + p</code> 为起始位置的字符串<strong>均无需再进行比较</strong>（<code>p</code> 的取值范围为 <code>[0, k]</code>）。<ul>
<li>以 <code>i + p</code> 为起始位置的字符串，一定存在以 <code>j + p</code> 为起始位置的字符串比它更优。</li>
</ul>
</li>
<li>事实上还避免了很多其他情况，但上述情况已经可以说明优化后解法的正确性。</li>
</ul>
</li>
</ul>
<h2 id="算法步骤："><a href="#算法步骤：" class="headerlink" title="算法步骤："></a>算法步骤：</h2><ul>
<li>优化后解法步骤如下：<ol>
<li>将字符串复制一倍。【此操作非必要】</li>
<li>初始化指针 <code>i = 0</code>、<code>j = 1</code>，初始化匹配长度 <code>k = 0</code>。</li>
<li>根据匹配位置 <code>k</code> 进行比较，根据比较结果移动相应指针，同时重置匹配长度 <code>k</code>；若指针移动后，两指针相同，则移动指针 <code>j</code>。</li>
<li>重复上一步骤直到比较结束。</li>
<li>最后的结果为 <code>min(i, j)</code>。【返回结果是字符串的起始索引】</li>
</ol>
</li>
</ul>
<h2 id="代码实现："><a href="#代码实现：" class="headerlink" title="代码实现："></a>代码实现：</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">minimal_representation</span><span class="params">(string&amp; s)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="type">int</span> n = s.<span class="built_in">size</span>();</span><br><span class="line">  <span class="type">int</span> k = <span class="number">0</span>, i = <span class="number">0</span>, j = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">while</span> (k &lt; n &amp;&amp; i &lt; n &amp;&amp; j &lt; n)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">if</span> (s[(i + k) % n] == s[(j + k) % n]) ++k;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">      s[(i + k) % n] &gt; s[(j + k) % n] ? i = i + k + <span class="number">1</span> : j = j + k + <span class="number">1</span>;</span><br><span class="line">      <span class="keyword">if</span> (i == j) j++;</span><br><span class="line">      k = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  i = <span class="built_in">min</span>(i, j);</span><br><span class="line">  <span class="keyword">return</span> i;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="部分题目："><a href="#部分题目：" class="headerlink" title="部分题目："></a>部分题目：</h2><ul>
<li>leetcode - 796.旋转字符串【比较两个字符串的最小表示结果是否相等】</li>
<li>leetcode - 899.有序队列</li>
</ul>
]]></content>
      <categories>
        <category>算法</category>
        <category>字符串</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
</search>
